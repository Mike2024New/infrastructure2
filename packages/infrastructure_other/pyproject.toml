[project]
name = "infrastructure-other"
version = "0.2.0"
description = "📗 Утилиты не вошедшие ни в одну из категорий (например подавитель логов c++ библиотек)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
