[project]
name = "infrastructure2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "inftastructure-git-client",
    "inftastructure-message-bus",
    "inftastructure-path-utils",
]


[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.uv.sources]
inftastructure-git-client = { workspace = true } # быстрый гит клиент
inftastructure-message-bus = { workspace = true } # шина сообщений
inftastructure-path-utils = { workspace = true } # обработчик файлов и путей
