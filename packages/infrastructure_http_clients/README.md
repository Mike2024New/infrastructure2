# HttpClient - http запросы и скачивание файлов.

### url для установки пакета (прописать в pyproject.toml целевого проекта)

```text
"infrastructure-http-clients @
git+https://github.com/Mike2024New/infrastructure2.git@main#subdirectory=packages/infrastructure_http_clients"
```