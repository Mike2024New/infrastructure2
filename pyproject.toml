[project]
name = "infrastructure2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "infrastructure-git-client",
    "infrastructure-message-bus",
    "infrastructure-path-utils",
]


[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.uv.sources]
infrastructure-git-client = { workspace = true } # быстрый гит клиент
infrastructure-message-bus = { workspace = true } # шина сообщений
infrastructure-path-utils = { workspace = true } # обработчик файлов и путей
