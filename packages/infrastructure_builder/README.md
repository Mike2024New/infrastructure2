# Builder - сборщик приложений в исполняемые файлы

### url для установки пакета (прописать в pyproject.toml целевого проекта)

```text
"infrastructure-builder @
git+https://github.com/Mike2024New/infrastructure2.git@main#subdirectory=packages/infrastructure_builder"
```