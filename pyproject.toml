[project]
name = "infrastructure2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "git-client",
    "message-bus",
    "path-utils",
]


[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.uv.sources]
git-client = { workspace = true } # быстрый гит клиент
message-bus = { workspace = true } # шина сообщений
path-utils = { workspace = true } # обработчик файлов и путей
